# gradepulse-backend/nixpacks.toml
[phases.setup]
aptPackages = ["python3", "python3-pip"]

[phases.install]
cmds = [
  "pip install --no-cache-dir -r requirements.txt"
]

[start]
cmd = "uvicorn main:app --host 0.0.0.0 --port $PORT"
