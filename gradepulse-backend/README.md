# GradePulse Backend

FastAPI + SQLModel backend for GradePulse.

## Tech Stack
- FastAPI
- SQLModel + SQLite
- LangChain + Groq
- Pandas for bulk operations

## Run Locally
```bash
pip install -r requirements.txt
uvicorn main:app --reload
```

## Endpoints
See `/docs` after running.